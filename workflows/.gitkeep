The wave loop lives here: claim, the residency barrier, worldgen, the turn loop, renew, the finish
per row, drain. It is blocked on docs/design.md (which owes K, N and the lease, and how a fixed task list
finishes K matches ending on different turns), on axon/docs/design.md's play half, and on the wave-turn spike
that turns those guesses into numbers. kalam's README §10 is the list.
