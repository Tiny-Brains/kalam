The wave claim channel lives here. It is deliberately NOT written yet: a claim channel with no wave
workflow behind it would take `pending` rows this replica cannot play and leave them to lapse until
the reap. It arrives with the workflow, once docs/design.md is written -- see the README, and
`kalam's README §10` item 1.6.
